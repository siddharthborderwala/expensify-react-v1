import React from 'react';
import AddExpensePage from './AddExpensePage';

const CreateExpensePage = (props) => (
	<div>
		<AddExpensePage {...props} />
	</div>
);

export default CreateExpensePage;